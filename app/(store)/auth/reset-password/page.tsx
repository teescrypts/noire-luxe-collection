import ResetPasswordView from "@/components/store/ResetPasswordView";

export const metadata = { title: "Reset Password" };

export default function ResetPasswordPage() {
  return <ResetPasswordView />;
}
